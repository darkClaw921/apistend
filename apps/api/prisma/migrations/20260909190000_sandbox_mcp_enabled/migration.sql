-- Доступ к песочнице по MCP включается пользователем явно.
--
-- Выключено по умолчанию, в том числе у существующих песочниц: агент с ключом
-- может вызывать моки и читать журнал запросов, где лежат тела запросов,
-- написанные человеком. Включать это молча за пользователя нельзя.
ALTER TABLE "sandboxes" ADD COLUMN "mcpEnabled" BOOLEAN NOT NULL DEFAULT false;
