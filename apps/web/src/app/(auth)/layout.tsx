/**
 * Экраны входа и регистрации рисуют собственный каркас: у каждого своя правая
 * витрина — терминал, метрики и комментарии отличаются. Общая часть каркаса
 * живёт в components/auth/AuthScreen.tsx.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children
}
